{
  "name": "monorepo",
  "description": "Monorepo for TC39 Temporal utilities.",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "lint": "biome check && turbo run lint",
    "lint:fix": "biome check --write --unsafe && turbo run lint -- --fix",
    "watch": "turbo watch build",
    "test": "bun test src/",
    "test:cov": "bun test src/ --coverage",
    "typedoc": "typedoc",
    "ci:version": "changeset version && bun update",
    "ci:publish": "for dir in packages/*; do (cd \"$dir\" && bun pm pack --filename package.tgz && bunx npm publish package.tgz --provenance --access public || true); done && changeset tag"
  },
  "keywords": [],
  "author": "Ian Macalinao <me@ianm.com>",
  "license": "Apache-2.0",
  "devDependencies": {
    "@biomejs/biome": "^2.3.10",
    "@changesets/cli": "^2.29.8",
    "@macalinao/biome-config": "^0.2.0",
    "turbo": "^2.8.12",
    "typedoc": "^0.28.1",
    "typescript": "^5.9.3"
  },
  "packageManager": "bun@1.3.1",
  "engines": {
    "node": ">=22",
    "bun": ">=1.0.0"
  }
}
